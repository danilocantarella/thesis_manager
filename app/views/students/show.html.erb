<% if signed_in_student? %>
<div class="alert alert-info">Benvenuto <strong><%= @student.nome %> <%= @student.cognome %></strong></div>

<%= link_to "Logout", logout_student_url,  class: "btn btn-primary" %>
<% else %>
<%= link_to "Login", root_url, class: "btn btn-primary" %>
<% end %>